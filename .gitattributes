# Normalize line endings to LF in the repository (WSL / Windows cross-platform)
* text=auto eol=lf

# Binaries — never touch
*.ico  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.svg  binary
*.woff binary
*.woff2 binary
